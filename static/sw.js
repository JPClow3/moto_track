/* Intentionally unused.
 *
 * The application serves /sw.js through apps.core.views.service_worker_view,
 * which renders templates/sw.js with the current APP_BUILD_ID. Keep service
 * worker behavior in that template so browser registration and tests exercise
 * the same code path.
 */
